package com.LeetCode.Solutions.isMatch;

public class isMatch {
    public boolean isMatch(String s, String p) {
        /*
        Pattern p = new Pattern(p);
        Matcher m = new Matcher();
        boolean result = m.match(s,p);
        d
         */
return false;
    }
}
